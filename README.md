# ms-andesstay-catalog

Spring Boot Java 17 + Oracle. Ejecuta `mvn test` o `mvn spring-boot:run` con `DB_URL`, `CATALOG_DB_USER`, `CATALOG_DB_PASSWORD` e `INTERNAL_TOKEN`. Escucha en 8082; solo debe ser accesible dentro de la red privada. Registra asignaciones por reserva y previene solapamientos de fechas.
# ms-andesstay-catalog
